

doctype html
html(lang='en')
  head
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title 환영합니다 | 배민상회
    link(rel="icon" href="/favicon.ico" type="image/x-icon")
    link(rel="stylesheet" href="/styles/common/globalstyle.css")
    style
      include ./welcome.css
  body
    main.welcome-main
      div.welcome-container
        img.main-logo(
          src="/assets/images/logo-ceo.svg"
          alt="main-logo"
        )
        h1.title-text #{currentUser.name}님, 반갑습니다.
        div.user-info-container
          div.user-info-wrapper
            label.user-info-label 이름
            p.user-info-content(id="name") #{currentUser.name}
          div.user-info-wrapper
            label.user-info-label 아이디
            p.user-info-content(id="userId") #{currentUser.userId}
          div.user-info-wrapper
            label.user-info-label 이메일
            p.user-info-content(id="email") #{currentUser.email}
          div.user-info-wrapper
            label.user-info-label 휴대폰
            p.user-info-content(id="phone") #{currentUser.phone}
        p.description 개인정보를 클릭해서 수정하실 수 있어요!
        div.button-wrapper
          a.link(
            class="sign-out"
            href="/api/auth/sign-out"
          ) 로그아웃
          button.delete-account-btn 탈퇴

    aside.modal
      .bg 
      .content 
        button.close 
          i.icon xmark
        .modal-main
        .button-wrapper
          button.ok 변경
          button.cancle 취소
    script(src="/dist/welcome.built.js")